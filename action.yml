# action.yml
name: 'Get LATEST'
author: blacktop
description: 'Get the contents of the LATEST|VERSION file in the project root'
outputs:
  version: # id of output
    description: 'The LATEST version'
runs:
  using: 'docker'
  image: 'Dockerfile'
branding:
  icon: 'hash'
  color: 'green'